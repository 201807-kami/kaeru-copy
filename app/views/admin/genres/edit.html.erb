<div class="container">
	<%= form_for @genre, url: admin_genre_path(@genre) do |f|%>
		<div class="card">
			<div class="card-header">
				<p>ジャンル編集</p>
			</div>
			<div class="card-body">
				<%= render 'form', f: f %>
				<div class="float-right">
					<%= f.submit '更新', class: 'btn btn-primary mr-2' %>
					<%= link_to 'キャンセル', admin_genres_path, class: 'btn btn-light' %>
				</div>
			</div>
		</div>
	<% end %>
</div>